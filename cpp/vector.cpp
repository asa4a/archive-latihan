#include <iostream>
#include <vector>
#include <sstream>
using namespace std;

int main(){

    // Input variables
    int T, N, M,D;
    
    // Read Jumlah Seleksi 
    cin >> T;

    //Read baris pertama 
    cin >> N >> M;

    // Read ID values
    vector<int> ID(N);
    for (int i = 0; i < N; i++) {
        cin >> ID[i];}
    
    // Output
    cout << "lala" << endl;

}